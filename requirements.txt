# Dynamic-Decoding 项目依赖
# 运行 run_evaluation_hf_gpt_oss.py 所需依赖（transformers-dynamic + gpqa evaluation）
# 安装: pip install -r requirements.txt

# 核心依赖
torch>=2.0.0
transformers>=4.35.2
accelerate>=0.26.0
numpy>=1.21.0
pandas>=1.5.0

# GPQA baselines
fire>=0.5.0
tqdm>=4.66.0
openai>=1.3.0
anthropic>=0.7.0
python-dotenv>=1.0.0
datasets>=2.15.0
evaluate>=0.4.0
sentencepiece>=0.1.99
pyrootutils>=1.0.4
scikit-learn>=1.3.0
wandb>=0.16.0
bs4>=0.0.1
seaborn>=0.13.0

# Evaluation & 可视化
matplotlib>=3.5.0

# transformers-dynamic 依赖
packaging>=20.0
huggingface-hub>=0.17.0
requests>=2.25.0
protobuf>=3.20.0

# 可选：vllm（仅 run_baseline_diamond_vllm、generate_vllm 需要）
# 若不使用 vllm 相关功能可注释掉
vllm>=0.2.0
